/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class celowniktextura : MonoBehaviour
{

    gunpoint cel;
    // Start is called before the first frame update
    void Start()
    {
        GameObject Celownik = GameObject.FindWithTag("Celownik");
        cel = Celownik.GetComponent<gunpoint>();
    }

    // Update is called once per frame
    void Update()
    {

        transform.position = cel.pozycjadlacelownika;


    }
}
*/